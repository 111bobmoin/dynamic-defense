# 交接记录

## 1. 目的

这份记录用于把当前目录 `dynamic-defense-main` 中已经完成的改动，回传到原始仓库。

本记录不是单纯描述当前页面效果，而是基于：

- 当前项目：`C:\Users\Administrator\Desktop\多模态\dynamic-defense-main`
- 上一个同级版本：`C:\Users\Administrator\Desktop\多模态\dynamic-defense-main-backup`

做差异梳理，目的是让后续人员能明确：

- 哪些改动是这次真正需要回传上游的源码改动
- 哪些差异只是环境、缓存、模型产物、虚拟环境或临时文件，不能上传
- 应该按什么粒度整理并提交到原始仓库

---

## 2. 对比结论

和 `dynamic-defense-main-backup` 对比后，当前项目的有效改动主要分成两部分：

1. `dynamic_defense_dashboard` 已从“动态防御占位页”改成“最小代价修复结果展示”可运行版本
2. 项目根目录新增了 `network_intrusion_detection_GAT` 源码目录，并且 dashboard 后端已经接入该目录的实验输出结果

真正应回传原始仓库的核心源码文件只有这些：

- `dynamic_defense_dashboard/server.py`
- `dynamic_defense_dashboard/static/app.js`
- `dynamic_defense_dashboard/static/index.html`
- `dynamic_defense_dashboard/static/defense.html`
- `dynamic_defense_dashboard/static/model-detail.html`
- `dynamic_defense_dashboard/static/styles.css`
- `network_intrusion_detection_GAT/` 下的源码与说明文件
- `network_intrusion_detection_GAT/outputs/` 下当前大屏实际依赖的结果目录
- `交接记录.md`

以下文件在本次对比中没有发生有效源码变更：

- `Agent.md`
- `codex.md`

---

## 3. outputs 依赖与不要上传的内容

当前需要修正一个关键判断：

- 不能再把 `network_intrusion_detection_GAT/outputs/` 一刀切视为“不上传的大体积产物”
- 当前大屏里的“最小代价修复/动态防御”模块，确实依赖其中的结果目录

### 3.1 当前大屏实际依赖哪些 outputs

后端实际读取路径见 [server.py](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/server.py:38)：

- `network_intrusion_detection_GAT/outputs/training/`
- `network_intrusion_detection_GAT/outputs/experiments/multi13_five_runs_20260529/`
- `network_intrusion_detection_GAT/outputs/results/`
- `network_intrusion_detection_GAT/outputs/verification/existing_samples/`

对应关系：

- `outputs/training/`
  - 用于读取 `metrics.json`
  - 用于生成首页和详情页里 `网络异常检测GAT` 的 Acc / Precision / Recall / F1
- `outputs/experiments/multi13_five_runs_20260529/`
  - 是当前大屏“最小代价修复”样本的主数据源
  - 首页外卡、独立防御页、详情页的轮换结果优先从这里读取
- `outputs/results/`
  - 如果 pinned experiments 不可用，后端会从这里回退读取动态防御样本
- `outputs/verification/existing_samples/`
  - 如果 `results/` 也不可用，后端会继续回退到这里

结论：

- 如果目标是“把当前项目的大屏原样回传到原始仓库，并且上传后不用重新跑实验就能直接展示”
- 那么 `network_intrusion_detection_GAT/outputs/` 里的相关目录也必须一起上传

### 3.2 最小必传范围

如果只追求“当前页面能按现在的方式正常显示”，最小建议上传：

- `network_intrusion_detection_GAT/outputs/training/20260511_182846/`
- `network_intrusion_detection_GAT/outputs/experiments/multi13_five_runs_20260529/`

原因：

- 前者提供当前 GAT 指标卡的训练/测试指标
- 后者提供当前大屏轮换展示的主样本来源

### 3.3 建议一起上传的回退范围

为了保留当前代码里的回退路径，建议额外上传：

- `network_intrusion_detection_GAT/outputs/results/`
- `network_intrusion_detection_GAT/outputs/verification/existing_samples/`

这样即使 pinned experiments 目录以后缺失，页面仍然有回退数据源。

### 3.4 仍然不要上传的内容

当前目录里依然存在很多不该回传原始仓库的差异。

明确排除：

- `.git/`
- `__pycache__/`
- `.runtime_cache/`
- `dynamic_defense_dashboard/dom_dump.html`
- `network_intrusion_detection_GAT/.venv/`
- `network_intrusion_detection_GAT/outputs/_internal/`
- 临时测试文件、浏览器抓取文件、运行期缓存

补充说明：

- `dynamic-defense-main-backup` 比当前目录多出很多 `log/`、`muti3/`、`graph/` 下的模型权重文件，这不代表当前代码缺功能，更多是包体差异。
- 当前目录比 `backup` 多出 `network_intrusion_detection_GAT/.venv/` 和相关依赖，这些只能本地使用，绝不能提交到原始仓库。
- 由于当前大屏确实依赖 GAT 的部分 `outputs`，所以这里不能再使用 `outputs/`、`*.pt`、`*.npy` 这类一刀切排除规则。

建议上游提交前先统一清理忽略规则，至少确保以下内容被忽略：

- `.venv/`
- `__pycache__/`
- `.runtime_cache/`
- `network_intrusion_detection_GAT/outputs/_internal/`

---

## 4. 当前项目相对 backup 的有效改动

### 4.1 后端：动态防御数据已接入 GAT 结果

文件：

- [server.py](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/server.py:1)

相对 `backup` 的关键变化：

- 新增 `network_intrusion_detection_GAT` 相关目录常量与轮换参数
- 新增 GAT 结果读取、JSON 读取、CSV 读取、类型转换等辅助函数
- 新增动态防御样本构建逻辑
- `build_dashboard_payload()` 中正式挂载 `dynamic_defense`
- `get_section_detail()` 中新增 `dynamic_defense` 分支，支持详情页接口

当前后端动态防御链路是：

1. `build_dynamic_defense_sample()`
2. `build_dynamic_defense_section()`
3. `/api/dashboard`
4. `/api/component-detail?section=dynamic_defense`

重点说明：

- 当前优先读取 `PINNED_GAT_EXPERIMENT_DIR`
  - `network_intrusion_detection_GAT/outputs/experiments/multi13_five_runs_20260529`
- 当前 GAT 指标卡读取 `network_intrusion_detection_GAT/outputs/training/**/metrics.json`
- 轮换周期为 `12s`
- 如果 pinned 实验不可用，则回退到 `network_intrusion_detection_GAT/outputs/results/`
- 如果 `results/` 也不可用，则再回退到 `network_intrusion_detection_GAT/outputs/verification/existing_samples/`

这意味着：

- 首页外部卡片
- 独立动态防御页
- 最小代价修复详情页

三者都已经共享同一个后端动态防御数据源

同时也意味着：

- 当前大屏不是只依赖源码
- 还依赖 `network_intrusion_detection_GAT/outputs/` 下的实际结果数据

---

### 4.2 首页：动态防御页签不再是占位模块

文件：

- [index.html](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/index.html:203)
- [app.js](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/app.js:1171)
- [styles.css](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/styles.css:3407)

相对 `backup` 的关键变化：

- 原来的“动态防御占位文案”已改为真实的“最小代价修复”展示区
- 首页顶部页签 `?tab=defense` 现在有实际内容，不再只是 placeholder
- 原本分离的“最小代价修复卡片”和“网络异常检测GAT卡片”已经合并成一个组合卡片
- 顶部英文标识改成了 `MINNUM COST REPAIR`
- 小标签 `Repair Plan / Core Node / Risk View` 向右对齐

首页外部卡片当前显示内容：

- 最小代价修复顺序
- 最小代价
- 异常节点
- GAT 模型测试指标
- 查看详情入口

补充：

- 首页外部卡片里的“异常节点”现在显示的是节点名单，不是数量
- 外卡数据会每 `12s` 自动刷新一次

---

### 4.3 独立动态防御页：从空页面改成完整结果页

文件：

- [defense.html](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/defense.html:1)
- [app.js](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/app.js:1101)

相对 `backup` 的关键变化：

- 原页面仅是“动态防御占位模块”
- 当前已经变成完整的“最小代价修复结果展示页”

新增内容包括：

- 运行时间标记
- 最小修复代价
- 异常节点
- 核心节点
- 修复步骤
- 修复优先级卡片
- 节点风险画像卡片
- 防御事件视图
- 关联链路
- GAT 指标卡

注意：

- 当前页里“异常节点”显示节点名单
- 当前页里“核心节点”仍显示数量
- 这是当前实现状态，不是文档错误

---

### 4.4 详情页：新增最小代价修复专用详情模式

文件：

- [model-detail.html](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/model-detail.html:1)
- [app.js](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/app.js:1585)

相对 `backup` 的关键变化：

- 原详情页只适用于 graph/log/muti3 这类组件监控
- 当前增加了 `dynamic_defense` 专用详情布局和渲染逻辑

新增能力：

- `section=dynamic_defense` 时切换到专用详情页布局
- 隐藏异构模型池
- 顶部标题改为“最小代价修复”
- 返回按钮可返回首页动态防御页签
- 详情页显示：
  - 最小代价
  - 异常节点
  - 核心节点
  - 最小代价修复顺序
  - 修复优先级
  - 节点风险画像

当前详情页字段含义：

- `异常节点`：显示节点名单
- `核心节点`：显示节点名单
- `最小代价修复顺序`：显示顺序链

这部分已经和首页外卡对齐：

- 最小代价修复顺序
- 最小代价
- 异常节点

且详情页现在也按 `12s` 自动轮换

---

### 4.5 前端同步刷新：外部页与详情页已联动

文件：

- [app.js](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/app.js:1440)

相对 `backup` 的关键变化：

- 新增 `defenseRotationTimer`
- 新增 `startDefenseRotation()`
- 首页 `dashboard`
- 独立 `defense`
- `model-detail`

都接入了相同周期的自动刷新逻辑

当前行为：

- 轮换周期：`12s`
- 首页动态防御卡片、独立防御页、详情页会跟随后端样本轮换一起更新

---

### 4.6 样式层：新增动态防御整套布局样式

文件：

- [styles.css](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/styles.css:3407)

相对 `backup` 的关键变化：

- 新增 defense 专属布局
- 新增首页组合卡片样式
- 新增详情页 defense summary 样式
- 新增节点长文本换行显示样式
- 新增响应式布局适配

主要受影响区域：

- `defense-layout-grid`
- `defense-entry-card`
- `defense-inline-model-section`
- `defense-kpi-grid`
- `defense-repair-grid`
- `defense-node-grid`

---

### 4.7 新增 GAT 源码目录

当前目录相对 `backup` 新增：

- `network_intrusion_detection_GAT/README.md`
- `network_intrusion_detection_GAT/OUTPUT_SPEC.md`
- `network_intrusion_detection_GAT/HANDOVER.md`
- `network_intrusion_detection_GAT/repair_plan.py`
- `network_intrusion_detection_GAT/src/repair.py`
- 以及其余源码文件

这是本次差异里的第二大块有效变更。

如果原始仓库此前没有这个目录，则需要决定上游是否接受：

1. 直接把 `network_intrusion_detection_GAT` 作为新模块引入
2. 只回传 dashboard 接入逻辑，不回传 GAT 源码

当前代码实际上已经依赖这个目录：

- 后端 `server.py` 会读取其 `outputs/`
- 前端文案和详情页都明确引用 `network_intrusion_detection_GAT`

所以如果原始仓库没有这个目录，上游至少要同步以下二选一：

- 一起引入 `network_intrusion_detection_GAT` 源码
- 或者把 `server.py` 中对该目录的依赖改成指向原始仓库已有的等价模块

---

## 5. 当前页面状态

首页动态防御页签入口：

- `http://127.0.0.1:8099/?tab=defense`

详情页入口：

- `http://127.0.0.1:8099/model-detail.html?section=dynamic_defense`

当前已确认的展示行为：

- 首页外部卡片会自动轮换
- 详情页会自动轮换
- 首页与详情页以下字段已同步：
  - 最小代价修复顺序
  - 最小代价
  - 异常节点

---

## 6. 回传原始仓库时建议的提交拆分

建议不要把所有内容打一包提交，建议至少拆成 4 个部分。

### 提交 1：dashboard 接入 dynamic_defense 后端数据

建议包含：

- `dynamic_defense_dashboard/server.py`

提交说明建议：

- 接入 `network_intrusion_detection_GAT` 输出
- 新增 `dynamic_defense` dashboard payload
- 新增 `dynamic_defense` component detail payload

### 提交 2：首页/独立页/详情页前端展示

建议包含：

- `dynamic_defense_dashboard/static/index.html`
- `dynamic_defense_dashboard/static/defense.html`
- `dynamic_defense_dashboard/static/model-detail.html`
- `dynamic_defense_dashboard/static/app.js`
- `dynamic_defense_dashboard/static/styles.css`

提交说明建议：

- 首页动态防御页签改为真实最小代价修复展示
- 新增详情页专用布局
- 前后页面联动轮换
- 展示字段与详情页对齐

### 提交 3：新增 GAT 模块源码

建议包含：

- `network_intrusion_detection_GAT/` 下源码和文档

但必须排除：

- `.venv/`

### 提交 4：新增当前大屏依赖的 GAT outputs

建议至少包含：

- `network_intrusion_detection_GAT/outputs/training/20260511_182846/`
- `network_intrusion_detection_GAT/outputs/experiments/multi13_five_runs_20260529/`

建议额外包含：

- `network_intrusion_detection_GAT/outputs/results/`
- `network_intrusion_detection_GAT/outputs/verification/existing_samples/`

但仍应排除：

- `network_intrusion_detection_GAT/.venv/`
- `network_intrusion_detection_GAT/outputs/_internal/`

---

## 7. 回传前需要人工确认的风险点

### 风险 1：`network_intrusion_detection_GAT` 是否应该整体进原始仓库

这是结构性问题，不应自动决定。

如果原始仓库已经有同名目录但内容不同，需要先手工比对后再合并。

### 风险 2：当前 `server.py` 绑定了固定实验目录

关键常量：

- `PINNED_GAT_EXPERIMENT_DIR`
- `PINNED_GAT_ROTATION_SECONDS`

这适合当前演示环境，但原始仓库未必希望写死路径和轮换策略。

上游如果要更通用，建议后续再改成：

- 配置项
- 环境变量
- 或自动发现策略

### 风险 3：当前大屏依赖部分 outputs，原始仓库如果不接收大文件会卡住

当前不是“只上传源码即可复现页面”的状态。

如果上游仓库不能直接接收这些结果目录，需要二选一：

1. 使用 Git LFS 或其他制品存储方式，把 `network_intrusion_detection_GAT/outputs/` 中的必需目录一并保留
2. 修改后端逻辑，让原始仓库在没有这些 outputs 的情况下也能退化展示或改为重新生成

### 风险 4：当前首页外卡与独立页字段含义不完全一致

当前实现状态：

- 首页外卡：异常节点显示节点名单
- 详情页：异常节点、核心节点都显示节点名单
- 独立防御页：异常节点显示节点名单，核心节点仍显示数量

如果原始仓库要统一口径，建议上传后再做一次字段语义清理。

### 风险 5：`MINNUM` 拼写按当前页面要求保留

首页顶部英文标识当前是：

- `MINNUM COST REPAIR`

详情页 stage kicker 当前是：

- `MINNUM-COST REPAIR`

这不是英文规范拼写，但这是当前页面需求，回传时不要擅自改回 `MINIMUM`。

---

## 8. 当前建议的上游排除清单

如果要把当前目录内容复制回原始仓库，建议显式排除以下路径：

```text
.git/
**/__pycache__/
**/.runtime_cache/
dynamic_defense_dashboard/dom_dump.html
network_intrusion_detection_GAT/.venv/
network_intrusion_detection_GAT/outputs/_internal/
```

---

## 9. 如果下一位继续处理，建议先看这些文件

优先顺序：

1. [交接记录.md](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/交接记录.md)
2. [server.py](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/server.py:1)
3. [app.js](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/app.js:1)
4. [index.html](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/index.html:203)
5. [model-detail.html](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/dynamic_defense_dashboard/static/model-detail.html:1)
6. [network_intrusion_detection_GAT/HANDOVER.md](C:/Users/Administrator/Desktop/多模态/dynamic-defense-main/network_intrusion_detection_GAT/HANDOVER.md)

---

## 10. 一句话总结

相对 `dynamic-defense-main-backup`，当前项目已经把 `dynamic_defense_dashboard` 的“动态防御占位模块”改造成了真实接入 `network_intrusion_detection_GAT` 结果的“最小代价修复”展示链路；当前大屏不仅依赖 dashboard 前后端源码和 GAT 源码，也依赖 `network_intrusion_detection_GAT/outputs/` 中的实际结果目录，因此回传上游时应上传必需的 outputs，同时继续排除虚拟环境、缓存和无关临时产物。
